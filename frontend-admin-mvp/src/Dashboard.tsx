import { Card, CardContent } from "@mui/material";

export const Dashboard = () => (
    <div>
        <h2>Entitlements</h2>
        <Card style={{ backgroundSize: 'cover', background: 'url(/binary.gif)' }}>
            <CardContent style={{ opacity: 0.3, justifyContent: "center", alignItems: "center", display: "flex" }}> <img width="420px" src="switch3.gif" /></CardContent>
        </Card>
    </div>
);